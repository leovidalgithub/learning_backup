# Free movement guide by mouse

>jQuery Animate
- ( top, left )
- queue: false
- queue( 'fx' ).length

- jQuery UI (for Color Animation)
- @Input()
- OnChanges : SimpleChanges ( implements )
- ngOnChanges for @Input() variables changes
